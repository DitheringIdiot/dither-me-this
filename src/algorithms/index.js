const color_reduction = require('./color-reduction')
const random = require('./random')
const ordered = require('./ordered')
const error_diffusion = require('./error-diffusion')

module.exports = {
    color_reduction,
    random,
    ordered,
    error_diffusion
}